AWSTemplateFormatVersion: '2010-09-09'
Description: >
  VPC with 3 subnets and VPC endpoints for HyperPod EKS cluster with RIG.
  No Internet Gateway or NAT Gateway - verifying RIG works without internet access.

Parameters:
  VpcCidr:
    Type: String
    Default: 10.0.0.0/16
    Description: CIDR block for the VPC

  HyperPodSubnetCidr:
    Type: String
    Default: 10.0.0.0/21
    Description: CIDR block for the HyperPod nodes subnet (/21 = 2046 hosts)

  EksSubnet1Cidr:
    Type: String
    Default: 10.0.8.0/28
    Description: CIDR block for EKS cluster subnet 1 (/28 = 14 hosts)

  EksSubnet2Cidr:
    Type: String
    Default: 10.0.8.16/28
    Description: CIDR block for EKS cluster subnet 2 (/28 = 14 hosts)

  HyperPodSubnetAz:
    Type: String
    Description: Availability zone ID for the HyperPod nodes subnet (e.g., usw2-az3)

  EksSubnet1Az:
    Type: String
    Description: Availability zone ID for EKS cluster subnet 1 (e.g., usw2-az3)

  EksSubnet2Az:
    Type: String
    Description: Availability zone ID for EKS cluster subnet 2 (e.g., usw2-az1)

  EnableInternetAccess:
    Type: String
    Default: 'false'
    AllowedValues:
      - 'true'
      - 'false'
    Description: Enable internet access via Internet Gateway and NAT Gateway

  PublicSubnetCidr:
    Type: String
    Default: 10.0.9.0/28
    Description: CIDR block for the public subnet (used for NAT Gateway)

Conditions:
  InternetAccessEnabled: !Equals [!Ref EnableInternetAccess, 'true']

Resources:

  # VPC
  VPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: !Ref VpcCidr
      EnableDnsSupport: true
      EnableDnsHostnames: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-vpc'

  # Subnets
  HyperPodSubnet:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref HyperPodSubnetCidr
      AvailabilityZoneId: !Ref HyperPodSubnetAz
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-hyperpod-subnet'

  EksSubnet1:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref EksSubnet1Cidr
      AvailabilityZoneId: !Ref EksSubnet1Az
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-eks-subnet-1'

  EksSubnet2:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref EksSubnet2Cidr
      AvailabilityZoneId: !Ref EksSubnet2Az
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-eks-subnet-2'

  # Route Tables
  HyperPodRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-hyperpod-rt'

  EksRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-eks-rt'

  HyperPodSubnetRouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref HyperPodSubnet
      RouteTableId: !Ref HyperPodRouteTable

  EksSubnet1RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref EksSubnet1
      RouteTableId: !Ref EksRouteTable

  EksSubnet2RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref EksSubnet2
      RouteTableId: !Ref EksRouteTable

  # Internet Access (conditional)

  PublicSubnet:
    Type: AWS::EC2::Subnet
    Condition: InternetAccessEnabled
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref PublicSubnetCidr
      AvailabilityZoneId: !Ref EksSubnet1Az
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-public-subnet'

  InternetGateway:
    Type: AWS::EC2::InternetGateway
    Condition: InternetAccessEnabled
    Properties:
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-igw'

  InternetGatewayAttachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Condition: InternetAccessEnabled
    Properties:
      VpcId: !Ref VPC
      InternetGatewayId: !Ref InternetGateway

  NatGatewayEip:
    Type: AWS::EC2::EIP
    Condition: InternetAccessEnabled
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-nat-eip'

  NatGateway:
    Type: AWS::EC2::NatGateway
    Condition: InternetAccessEnabled
    Properties:
      AllocationId: !GetAtt NatGatewayEip.AllocationId
      SubnetId: !Ref PublicSubnet
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-nat-gw'

  PublicRouteTable:
    Type: AWS::EC2::RouteTable
    Condition: InternetAccessEnabled
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-public-rt'

  PublicRoute:
    Type: AWS::EC2::Route
    Condition: InternetAccessEnabled
    DependsOn: InternetGatewayAttachment
    Properties:
      RouteTableId: !Ref PublicRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref InternetGateway

  PublicSubnetRouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: InternetAccessEnabled
    Properties:
      SubnetId: !Ref PublicSubnet
      RouteTableId: !Ref PublicRouteTable

  HyperPodNatRoute:
    Type: AWS::EC2::Route
    Condition: InternetAccessEnabled
    Properties:
      RouteTableId: !Ref HyperPodRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGateway

  EksNatRoute:
    Type: AWS::EC2::Route
    Condition: InternetAccessEnabled
    Properties:
      RouteTableId: !Ref EksRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGateway

  # # Security Group for VPC Endpoints
  # VpcEndpointSecurityGroup:
  #   Type: AWS::EC2::SecurityGroup
  #   Properties:
  #     GroupDescription: Security group for VPC endpoints
  #     VpcId: !Ref VPC
  #     SecurityGroupIngress:
  #       - IpProtocol: tcp
  #         FromPort: 443
  #         ToPort: 443
  #         CidrIp: !Ref VpcCidr
  #     Tags:
  #       - Key: Name
  #         Value: !Sub '${AWS::StackName}-vpce-sg'

  # VPC Endpoints

  S3GatewayEndpoint:
    Type: AWS::EC2::VPCEndpoint
    Properties:
      VpcId: !Ref VPC
      ServiceName: !Sub 'com.amazonaws.${AWS::Region}.s3'
      VpcEndpointType: Gateway
      RouteTableIds:
        - !Ref HyperPodRouteTable
        - !Ref EksRouteTable

  # LambdaEndpoint:
  #   Type: AWS::EC2::VPCEndpoint
  #   Properties:
  #     VpcId: !Ref VPC
  #     ServiceName: !Sub 'com.amazonaws.${AWS::Region}.lambda'
  #     VpcEndpointType: Interface
  #     PrivateDnsEnabled: true
  #     SubnetIds:
  #       - !Ref EksSubnet1
  #       - !Ref EksSubnet2
  #     SecurityGroupIds:
  #       - !Ref VpcEndpointSecurityGroup
  #
  # SqsEndpoint:
  #   Type: AWS::EC2::VPCEndpoint
  #   Properties:
  #     VpcId: !Ref VPC
  #     ServiceName: !Sub 'com.amazonaws.${AWS::Region}.sqs'
  #     VpcEndpointType: Interface
  #     PrivateDnsEnabled: true
  #     SubnetIds:
  #       - !Ref EksSubnet1
  #       - !Ref EksSubnet2
  #     SecurityGroupIds:
  #       - !Ref VpcEndpointSecurityGroup
  #
  # ApsWorkspacesEndpoint:
  #   Type: AWS::EC2::VPCEndpoint
  #   Properties:
  #     VpcId: !Ref VPC
  #     ServiceName: !Sub 'com.amazonaws.${AWS::Region}.aps-workspaces'
  #     VpcEndpointType: Interface
  #     PrivateDnsEnabled: true
  #     SubnetIds:
  #       - !Ref EksSubnet1
  #       - !Ref EksSubnet2
  #     SecurityGroupIds:
  #       - !Ref VpcEndpointSecurityGroup
  #
  # GrafanaWorkspaceEndpoint:
  #   Type: AWS::EC2::VPCEndpoint
  #   Properties:
  #     VpcId: !Ref VPC
  #     ServiceName: !Sub 'com.amazonaws.${AWS::Region}.grafana-workspace'
  #     VpcEndpointType: Interface
  #     PrivateDnsEnabled: true
  #     SubnetIds:
  #       - !Ref EksSubnet1
  #       - !Ref EksSubnet2
  #     SecurityGroupIds:
  #       - !Ref VpcEndpointSecurityGroup

Outputs:
  VpcId:
    Description: VPC ID
    Value: !Ref VPC

  HyperPodSubnetId:
    Description: Subnet ID for HyperPod nodes
    Value: !Ref HyperPodSubnet

  EksSubnet1Id:
    Description: Subnet ID for EKS cluster (AZ1)
    Value: !Ref EksSubnet1

  EksSubnet2Id:
    Description: Subnet ID for EKS cluster (AZ2)
    Value: !Ref EksSubnet2

  # VpcEndpointSecurityGroupId:
  #   Description: Security group ID for VPC endpoints
  #   Value: !Ref VpcEndpointSecurityGroup
